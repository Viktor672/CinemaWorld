import { BlurValidator } from './blur-validator';

describe('BlurValidator', () => {
  it('should create an instance', () => {
    const directive = new BlurValidator();
    expect(directive).toBeTruthy();
  });
});
