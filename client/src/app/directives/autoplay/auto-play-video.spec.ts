import { AutoPlayVideo } from './auto-play-video.directive';

describe('AutoPlayVideo', () => {
  it('should create an instance', () => {
    const directive = new AutoPlayVideo();
    expect(directive).toBeTruthy();
  });
});
